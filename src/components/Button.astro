---
const { id, styles } = Astro.props;
---

<button
  type="button"
  id={id}
  class="transition"
  class:list={[
    " border border-white rounded-xl px-4 py-2 hover:bg-white hover:text-slate-900 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-slate-900",
    styles,
  ]}
>
  <slot />
</button>
